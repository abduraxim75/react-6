import React from 'react';
import loading from "../assets/animtion.svg"
const Loading = () => {
    return (
        <div>
            <img className='loading' src={loading}/>
        </div>
    );
}

export default Loading;
